To run, execute pip install -r requirements.txt
then run python app.py

You can also run the exe file.

The logins are as follows:

tester1   123
tester2   123

The first user is an administrator and the second is a normal user.
